apiVersion: v1
kind: Secret
metadata:
  name: hms-postgres-credentials
  namespace: data-pipeline
type: Opaque
stringData:
  # Lab-only credentials. Do not reuse anywhere real.
  POSTGRES_USER: hive
  POSTGRES_PASSWORD: hive
  POSTGRES_DB: metastore
